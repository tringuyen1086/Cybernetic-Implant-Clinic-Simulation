package clinic;

/**
 * The {@link RoomType} enum represents the different types of rooms
 * available in the clinic. Each room has a specific function
 * based on its type, such as waiting, examination, or procedure rooms.
 */
public enum RoomType {
  WAITING,
  EXAM,
  PROCEDURE;
}
